   
    function Person({name, age, children }) {
                return (
                    <div>
                        <h1>{name}</h1>
                        <p>{ age }</p>
                        <p>{children}</p>
                    </div>
                )}

    export default Person;